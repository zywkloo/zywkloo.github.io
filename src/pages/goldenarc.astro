---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { GOLDENARC_LINKEDIN_URL, GOLDENARC_NAME, SITE_URL } from '../consts';

const products = [
	{
		name: 'ARC EDF & BDF Viewer',
		platform: 'iPadOS + macOS',
		description: 'Open EDF, EDF+ and BDF recordings, explore every channel, and inspect file metadata locally.',
		status: 'Now on App Store',
		statusTone: 'live',
		href: '/edfviewer',
		icon: '/images/goldenarc/arc-edf-viewer.png',
	},
	{
		name: 'ARC Scanner',
		platform: 'iPhone + iPad',
		description: 'Scan or import pages, review them, and export useful document formats with on-device processing.',
		status: 'In development',
		statusTone: 'building',
		icon: '/images/goldenarc/arc-scanner.png',
	},
	{
		name: 'ARC Markdown',
		platform: 'iPhone + iPad',
		description: 'Open local Markdown and TXT files as readable documents, with clean front matter and a reliable outline.',
		status: 'In development',
		statusTone: 'building',
		monogram: 'MD',
	},
	{
		name: 'ARC PDF Studio',
		platform: 'iPad + Mac',
		description: 'Read, annotate, arrange, merge, and export standard PDFs without locking work into one app.',
		status: 'Architecture planned',
		statusTone: 'planned',
		icon: '/images/goldenarc/arc-pdf-studio.png',
	},
];

const organizationLd = {
	'@context': 'https://schema.org',
	'@type': 'Organization',
	'@id': `${SITE_URL}/#goldenarc`,
	name: GOLDENARC_NAME,
	url: `${SITE_URL}/goldenarc/`,
	sameAs: [GOLDENARC_LINKEDIN_URL],
};
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title="GoldenARC Digital | One Core. Many Arcs."
			description="Plug Once. Grow Everywhere. GoldenARC Digital builds local-first Apple platform tools on a modular Swift package architecture, including ARC EDF & BDF Viewer, ARC Scanner, ARC Markdown, and ARC PDF Studio."
		/>
		<script type="application/ld+json" is:inline set:html={JSON.stringify(organizationLd)} />
	</head>
	<body>
		<Header />
		<div class="arc-stage">
			<main class="arc-page">
			<section class="hero">
				<div class="brand-lockup">
					<img src="/images/goldenarc/brand-mark.png" alt="" width="64" height="64" />
					<span>GoldenARC Digital</span>
				</div>
				<h1>One Core. Many Arcs.</h1>
				<p class="lede">
					Plug Once. Grow Everywhere. GoldenARC tools stay focused at the product layer while shared
					capabilities live in small, testable Swift Package products with explicit dependency boundaries.
				</p>
			</section>

			<section aria-labelledby="tools-heading">
				<div class="section-heading">
					<div>
						<p class="eyebrow">The ARC series</p>
						<h2 id="tools-heading">Choose a tool</h2>
					</div>
					<p>Separate products at different stages, built around the same engineering discipline.</p>
				</div>

				<div class="product-grid">
					{products.map((product) => {
						const CardTag = product.href ? 'a' : 'article';
						return (
							<CardTag
								href={product.href}
								class:list={['product-card', { available: product.href }]}
								aria-label={product.href ? `Explore ${product.name}` : undefined}
							>
								<div class="product-visual">
									{product.icon ? (
										<img src={product.icon} alt={`${product.name} app icon`} width="512" height="512" loading="lazy" />
									) : (
										<div class="product-monogram arc-markdown-icon" role="img" aria-label={`${product.name} app icon concept`}>
											<span class="markdown-wordmark">.{product.monogram}</span>
											<span class="markdown-ruler" aria-hidden="true"></span>
											<span class="markdown-lines" aria-hidden="true"><i></i><i></i><i></i></span>
										</div>
									)}
								</div>
								<div class="product-meta">
									<span class:list={['status', product.statusTone, { 'app-store-status': product.statusTone === 'live' }]}>
										{product.statusTone === 'live' && (
											<svg class="apple-mark" viewBox="0 0 170 170" aria-hidden="true">
												<path d="M150.37 130.25c-2.45 5.66-5.35 10.87-8.71 15.66-4.58 6.53-8.33 11.05-11.22 13.56-4.48 4.12-9.28 6.23-14.42 6.35-3.69 0-8.14-1.05-13.32-3.18-5.19-2.12-9.97-3.17-14.34-3.17-4.58 0-9.49 1.05-14.75 3.17-5.26 2.13-9.5 3.24-12.74 3.35-4.35.13-9.16-1.9-14.42-6.08-3.7-3.04-7.59-7.71-11.66-14.01-6.19-9.56-10.9-20.2-14.13-31.92-3.23-11.72-4.85-22.95-4.85-33.69 0-14.13 3.63-25.75 10.9-34.85 7.27-9.1 16.31-13.78 27.12-14.06 4.35 0 9.28 1.13 14.79 3.38 5.51 2.26 9.4 3.44 11.66 3.56 1.85 0 5.86-1.25 12.02-3.74 6.16-2.5 11.53-3.63 16.11-3.39 12.08.75 21.65 5.38 28.71 13.9-10.88 6.64-16.19 15.54-15.93 26.7.26 8.71 3.59 16.03 10 21.96 6.41 5.93 13.9 9.38 22.47 10.35-2.28 6.95-5.07 14.18-8.37 21.68zM119.22 31.84c0-7.38 2.65-14.28 7.95-20.7 5.3-6.42 11.83-10.45 19.59-12.09.87 7.06-1.57 14.01-7.32 20.85-5.75 6.84-12.49 10.82-20.22 11.94z" />
											</svg>
										)}
										{product.status}
									</span>
									<span>{product.platform}</span>
								</div>
								<h3>{product.name}</h3>
								<p>{product.description}</p>
								{product.href ? (
									<span class="product-link">
										Explore the app <span class="arrow" aria-hidden="true">→</span>
									</span>
								) : (
									<span class="product-link muted">Details will follow</span>
								)}
							</CardTag>
						);
					})}
				</div>
			</section>

			<section class="architecture" aria-labelledby="architecture-heading">
				<div class="section-heading architecture-heading">
					<div>
						<p class="eyebrow">Engineering under the apps</p>
						<h2 id="architecture-heading">One-way dependencies. No umbrella package.</h2>
					</div>
					<p>
						The graph separates shipped paths, planned domain packages, and future optional import or export work,
						so roadmap ideas are visible without pretending they already exist.
					</p>
				</div>

				<div class="architecture-panel">
					<div class="diagram-legend" aria-label="Architecture diagram legend">
						<span><i class="legend-line solid"></i> Implemented / integrated</span>
						<span><i class="legend-line dashed"></i> Planned dependency</span>
						<span><i class="legend-line future"></i> Future optional path</span>
						<span><i class="legend-box"></i> Independent app stack</span>
					</div>

					<svg class="dependency-graph" viewBox="0 0 1080 770" role="img" aria-labelledby="graph-title graph-desc">
						<title id="graph-title">GoldenARC product and Swift package dependency graph</title>
						<desc id="graph-desc">
							ARC Scanner uses DocumentPipelineKit, which uses ARC Shared Infra and Apple frameworks.
							ARC PDF Studio is planned to use PDFDocumentKit, DocumentPipelineKit, and InkObjectKit.
							ARC Markdown currently uses an independent reader core and may later add optional OCR and PDF import through DocumentPipelineKit, plus paged PDF export through future PDFExportKit.
							The ARC EDF &amp; BDF Viewer remains an independent specialized app built directly on Apple frameworks and may later use future PDFExportKit for paged waveform and metadata reports.
							PDFExportKit is a planned low-level generator for new paginated PDFs; it is separate from PDFDocumentKit, which owns existing PDF composition, annotations, forms, and text.
						</desc>
						<defs>
							<marker id="arrow-solid" markerWidth="10" markerHeight="10" refX="8" refY="4" orient="auto" markerUnits="strokeWidth">
								<path d="M0,0 L8,4 L0,8 Z" class="arrow-head solid" />
							</marker>
							<marker id="arrow-planned" markerWidth="10" markerHeight="10" refX="8" refY="4" orient="auto" markerUnits="strokeWidth">
								<path d="M0,0 L8,4 L0,8 Z" class="arrow-head planned" />
							</marker>
							<marker id="arrow-future" markerWidth="10" markerHeight="10" refX="8" refY="4" orient="auto" markerUnits="strokeWidth">
								<path d="M0,0 L8,4 L0,8 Z" class="arrow-head future" />
							</marker>
						</defs>

						<text class="layer-label" x="18" y="112">PRODUCT APPS</text>
						<text class="layer-label" x="18" y="278">PDF DOMAIN</text>
						<text class="layer-label" x="18" y="438">DOCUMENT DOMAIN</text>
						<text class="layer-label" x="18" y="598">OUTPUT + NEUTRAL INFRA</text>
						<text class="layer-label" x="18" y="730">APPLE PLATFORM</text>

						<g class="graph-edges">
							<path class="edge solid" marker-end="url(#arrow-solid)" d="M250 154 C250 390 220 520 250 690" />
							<path class="edge solid" marker-end="url(#arrow-solid)" d="M480 154 L500 385" />
							<path class="edge solid" marker-end="url(#arrow-solid)" d="M710 154 C710 380 675 520 710 690" />
							<path class="edge solid" marker-end="url(#arrow-solid)" d="M500 475 L500 545" />
							<path class="edge solid" marker-end="url(#arrow-solid)" d="M500 635 L500 690" />

							<path class="edge planned" marker-end="url(#arrow-planned)" d="M940 154 L920 225" />
							<path class="edge planned" marker-end="url(#arrow-planned)" d="M865 154 C710 235 675 320 620 385" />
							<path class="edge planned" marker-end="url(#arrow-planned)" d="M1000 154 L940 545" />
							<path class="edge planned" marker-end="url(#arrow-planned)" d="M800 270 C700 300 675 360 620 405" />
							<path class="edge planned" marker-end="url(#arrow-planned)" d="M940 635 L940 690" />
							<path class="edge future" marker-end="url(#arrow-future)" d="M250 154 C280 305 585 392 700 545" />
							<path class="edge future" marker-end="url(#arrow-future)" d="M680 154 C638 225 620 310 560 385" />
							<path class="edge future" marker-end="url(#arrow-future)" d="M710 154 C720 290 705 405 700 545" />
							<path class="edge future" marker-end="url(#arrow-future)" d="M620 456 C650 478 685 503 700 545" />
							<path class="edge future" marker-end="url(#arrow-future)" d="M800 315 C765 370 720 465 700 545" />
						</g>

						<g class="graph-node independent">
							<rect x="150" y="65" width="200" height="89" rx="18" />
							<text class="node-title" x="170" y="101">EDF &amp; BDF Viewer</text>
							<text class="node-meta" x="170" y="128">Signals now · PDF later</text>
						</g>
						<g class="graph-node implemented">
							<rect x="380" y="65" width="200" height="89" rx="18" />
							<text class="node-title" x="400" y="101">ARC Scanner</text>
							<text class="node-meta" x="400" y="128">App path integrated</text>
						</g>
						<g class="graph-node independent">
							<rect x="610" y="65" width="200" height="89" rx="18" />
							<text class="node-title" x="630" y="101">ARC Markdown</text>
							<text class="node-meta" x="630" y="128">Reader core now · import later</text>
						</g>
						<g class="graph-node planned">
							<rect x="840" y="65" width="200" height="89" rx="18" />
							<text class="node-title" x="860" y="101">ARC PDF Studio</text>
							<text class="node-meta" x="860" y="128">Architecture planned</text>
						</g>

						<g class="graph-node planned">
							<rect x="800" y="225" width="240" height="90" rx="18" />
							<text class="node-title" x="824" y="261">PDFDocumentKit</text>
							<text class="node-meta" x="824" y="288">Composition · annotation · text</text>
						</g>
						<g class="graph-node implemented emphasized">
							<rect x="380" y="385" width="240" height="90" rx="18" />
							<text class="node-title" x="404" y="421">DocumentPipelineKit</text>
							<text class="node-meta" x="404" y="448">Contracts · pipeline · export</text>
						</g>
						<g class="future-label">
							<rect x="565" y="255" width="150" height="44" rx="14" />
							<text x="640" y="273" text-anchor="middle">Future optional</text>
							<text x="640" y="290" text-anchor="middle">OCR / PDF import</text>
						</g>
						<g class="graph-node implemented">
							<rect x="320" y="545" width="240" height="90" rx="18" />
							<text class="node-title" x="344" y="581">ARC Shared Infra</text>
							<text class="node-meta" x="344" y="608">File intake · scan · OCR</text>
						</g>
						<g class="graph-node future">
							<rect x="590" y="545" width="220" height="90" rx="18" />
							<text class="node-title" x="614" y="581">PDFExportKit</text>
							<text class="node-meta" x="614" y="608">Future · pages · PDF writing</text>
						</g>
						<g class="graph-node planned">
							<rect x="840" y="545" width="200" height="90" rx="18" />
							<text class="node-title" x="860" y="581">InkObjectKit</text>
							<text class="node-meta" x="860" y="608">Ink · objects · Pencil</text>
						</g>
						<g class="graph-node platform">
							<rect x="150" y="690" width="890" height="62" rx="18" />
							<text class="node-title platform-title" x="595" y="727" text-anchor="middle">Apple frameworks · Files · Vision · VisionKit · PDFKit · PencilKit</text>
						</g>
					</svg>

					<div class="dependency-list" aria-hidden="true">
						<div><strong>ARC Scanner</strong><span>↓ DocumentPipelineKit ↓ ARC Shared Infra ↓ Apple frameworks</span></div>
						<div><strong>ARC PDF Studio <em>planned</em></strong><span>↓ PDFDocumentKit + DocumentPipelineKit + InkObjectKit ↓ Apple frameworks. Future optional: PDFExportKit only for new paginated output.</span></div>
						<div><strong>ARC Markdown</strong><span>Current: independent Markdown reader core ↓ Apple frameworks. Future optional: OCR / PDF import through DocumentPipelineKit; paged PDF export through PDFExportKit.</span></div>
						<div><strong>EDF &amp; BDF Viewer</strong><span>Current: independent specialized stack ↓ Apple frameworks. Future optional: paged waveform / metadata report through PDFExportKit.</span></div>
					</div>

					<div class="architecture-note">
						<strong>The important part is the boundary, not the box count.</strong>
						<p>
							Apps depend downward; infrastructure never reaches back into a product. Platform results are
							normalized into contracts, concrete providers are injected at each app's composition root, and
							consumers select individual SPM products instead of linking an all-in-one framework.
						</p>
					</div>
				</div>

				<div class="evidence-grid" aria-label="Architecture implementation evidence">
					<div><strong>12</strong><span>selectable SPM library products across the implemented shared packages</span></div>
					<div><strong>66</strong><span>passing package tests at the documented Scanner integration baseline</span></div>
					<div><strong>iOS 16 → 26+</strong><span>baseline OCR with capability-gated structured document recognition</span></div>
					<div><strong>Local first</strong><span>core intake, scan, OCR, export, and file work require no account or cloud service</span></div>
				</div>
			</section>

			<section class="principles" aria-labelledby="principles-heading">
				<p class="eyebrow">Product and engineering principles</p>
				<h2 id="principles-heading">Built as packages. Shipped as tools.</h2>
				<div class="principle-grid">
					<div><strong>Local first</strong><span>Core file work stays on your device.</span></div>
					<div><strong>Focused products</strong><span>Each app has a clear job and links only the capabilities it needs.</span></div>
					<div><strong>Portable output</strong><span>Exports remain useful outside the ARC family.</span></div>
				</div>
			</section>
			</main>
		</div>
		<Footer />
	</body>
</html>

<style>
	.arc-stage {
		background:
			radial-gradient(42rem 26rem at 8% 8%, rgba(27, 135, 201, 0.18), transparent 72%),
			radial-gradient(38rem 24rem at 94% 26%, rgba(232, 93, 26, 0.13), transparent 74%),
			linear-gradient(180deg, #e9eff5 0%, #f5f7fa 48%, #e8eef4 100%);
		border-bottom: 1px solid #d6dee7;
	}
	.arc-page {
		--arc-ink: #111827;
		--arc-muted: #556274;
		--arc-teal: #087a61;
		--arc-teal-dark: #065f4d;
		--arc-mint: #2de0a8;
		--arc-navy: #131c33;

		width: min(1080px, calc(100% - 2.5rem));
		max-width: none;
		padding: 5.5rem 0 6rem;
	}
	.hero {
		max-width: 760px;
		padding-bottom: 5rem;
	}
	.brand-lockup {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		margin-bottom: 1.35rem;
		color: var(--arc-ink);
		font-size: 1rem;
		font-weight: 700;
		letter-spacing: -0.01em;
		text-transform: none;
	}
	.brand-lockup img {
		width: 3rem;
		height: 3rem;
		border-radius: 0;
		filter: drop-shadow(0 8px 12px rgba(16, 23, 31, 0.14));
	}
	.arc-page .eyebrow {
		margin: 0 0 0.8rem;
		color: var(--arc-teal);
	}
	h1 {
		max-width: 720px;
		font-size: clamp(3rem, 8vw, 5.8rem);
		line-height: 0.98;
		letter-spacing: -0.055em;
	}
	.lede {
		max-width: 690px;
		margin: 1.5rem 0 0;
		color: var(--arc-muted);
		font-size: clamp(1.08rem, 2vw, 1.28rem);
		line-height: 1.65;
	}
	.section-heading {
		display: flex;
		align-items: end;
		justify-content: space-between;
		gap: 2rem;
		margin-bottom: 1.5rem;
	}
	.section-heading h2,
	.principles h2 {
		font-size: clamp(2rem, 4vw, 3rem);
	}
	.section-heading > p {
		max-width: 330px;
		margin: 0 0 0.35rem;
		color: var(--arc-muted);
		font-size: 0.95rem;
		text-align: right;
	}
	.product-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
	}
	.product-card {
		display: flex;
		min-height: 430px;
		flex-direction: column;
		padding: 1.4rem;
		background: rgba(255, 255, 255, 0.94);
		border: 1px solid #cfd8e3;
		border-radius: 22px;
		box-shadow: var(--shadow-sm);
		text-decoration: none;
		color: inherit;
		transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
	}
	.product-card.available {
		border-color: rgba(8, 122, 97, 0.3);
		box-shadow: 0 12px 35px rgba(17, 24, 39, 0.07);
		cursor: pointer;
	}
	.product-card.available:hover {
		transform: translateY(-5px);
		border-color: rgba(8, 122, 97, 0.55);
		box-shadow: 0 24px 52px rgba(8, 122, 97, 0.16), 0 8px 24px rgba(17, 24, 39, 0.08);
	}
	.product-visual {
		display: grid;
		width: 100%;
		height: 176px;
		place-items: center;
		margin-bottom: 1.25rem;
		overflow: hidden;
		background:
			radial-gradient(circle at 50% 30%, rgba(45, 224, 168, 0.14), transparent 58%),
			var(--arc-navy);
		border-radius: 15px;
		transition: background 0.3s ease;
	}
	.product-card.available:hover .product-visual {
		background:
			radial-gradient(circle at 50% 30%, rgba(45, 224, 168, 0.26), transparent 64%),
			#0d1527;
	}
	.product-visual img {
		width: 126px;
		height: 126px;
		border: 1px solid rgba(255, 255, 255, 0.18);
		border-radius: 27px;
		box-shadow: 0 18px 32px rgba(5, 10, 25, 0.38);
		object-fit: cover;
		transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.3s ease, filter 0.3s ease;
	}
	.product-card.available:hover .product-visual img {
		transform: scale(1.08) translateY(-2px);
		box-shadow: 0 24px 42px rgba(5, 10, 25, 0.55);
		filter: brightness(1.05);
	}
	.product-monogram {
		position: relative;
		width: 126px;
		height: 126px;
		overflow: hidden;
		background: #080e1d;
		border: 1px solid rgba(255, 255, 255, 0.22);
		border-radius: 27px;
		box-shadow: 0 18px 32px rgba(5, 10, 25, 0.38);
	}
	.markdown-wordmark {
		position: absolute;
		top: 13px;
		left: 50%;
		transform: translateX(-50%);
		color: #f8fafc;
		font-family: "DIN Condensed", "Arial Narrow", sans-serif;
		font-size: 3.55rem;
		font-stretch: condensed;
		font-weight: 700;
		letter-spacing: 0.015em;
		line-height: 1;
		white-space: nowrap;
	}
	.markdown-ruler {
		position: absolute;
		top: 72px;
		left: 17px;
		width: 92px;
		height: 8px;
		background: repeating-linear-gradient(90deg, #dbe4ef 0 1px, transparent 1px 15px);
		border-top: 1px solid #dbe4ef;
	}
	.markdown-ruler::after {
		position: absolute;
		top: -1px;
		left: 50%;
		width: 1px;
		height: 11px;
		background: #dbe4ef;
		content: "";
	}
	.markdown-lines {
		position: absolute;
		top: 89px;
		left: 19px;
		display: grid;
		width: 88px;
		gap: 4px;
		transform: rotate(1.5deg);
	}
	.markdown-lines i {
		display: block;
		height: 4px;
		background: #2de0a8;
		border-radius: 2px;
	}
	.markdown-lines i:nth-child(1) {
		width: 68%;
	}
	.markdown-lines i:nth-child(2) {
		width: 100%;
	}
	.markdown-lines i:nth-child(3) {
		width: 82%;
	}
	.product-meta {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.5rem;
		color: var(--arc-muted);
		font-size: 0.8rem;
		font-weight: 700;
		letter-spacing: 0;
		text-transform: none;
	}
	.status {
		padding: 0.18rem 0.5rem;
		background: var(--surface-muted);
		border-radius: 999px;
	}
	.status.live {
		background: #daf7ed;
		color: var(--arc-teal-dark);
	}
	.status.live.app-store-status {
		display: inline-flex;
		align-items: center;
		gap: 0.38rem;
		padding: 0.31rem 0.62rem;
		background: linear-gradient(145deg, #151719, #34383d);
		border: 1px solid rgba(255, 255, 255, 0.2);
		box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.12), 0 5px 12px rgba(0, 0, 0, 0.18);
		color: #f8fafc;
		letter-spacing: 0.015em;
	}
	.apple-mark {
		width: 0.72rem;
		height: 0.72rem;
		fill: currentColor;
		flex: 0 0 auto;
	}
	.status.review {
		background: #e0f2fe;
		color: #075985;
	}
	.status.building {
		background: #dcfce7;
		color: #166534;
	}
	.status.planned {
		background: #f1f5f9;
		color: #475569;
	}
	.product-card h3 {
		margin: 1rem 0 0.55rem;
		font-size: 1.35rem;
		transition: color 0.2s ease;
	}
	.product-card.available:hover h3 {
		color: var(--arc-teal);
	}
	.product-card p {
		margin: 0;
		color: var(--arc-muted);
		font-size: 0.94rem;
		line-height: 1.55;
	}
	.product-link {
		margin-top: auto;
		padding-top: 1.5rem;
		color: var(--arc-teal);
		font-size: 0.9rem;
		font-weight: 700;
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
	}
	.product-card.available:hover .product-link {
		color: var(--arc-teal-dark);
	}
	.product-link .arrow {
		display: inline-block;
		transition: transform 0.2s ease;
	}
	.product-card.available:hover .product-link .arrow {
		transform: translateX(4px);
	}
	.product-link.muted {
		color: var(--arc-muted);
	}
	.architecture {
		margin-top: 6rem;
	}
	.architecture-heading {
		align-items: end;
	}
	.architecture-panel {
		padding: 1.25rem;
		background: rgba(255, 255, 255, 0.92);
		border: 1px solid #cfd8e3;
		border-radius: 24px;
		box-shadow: 0 24px 60px rgba(17, 24, 39, 0.08);
	}
	.diagram-legend {
		display: flex;
		flex-wrap: wrap;
		gap: 0.8rem 1.35rem;
		padding: 0.45rem 0.35rem 1rem;
		color: var(--arc-muted);
		font-size: 0.78rem;
		font-weight: 700;
	}
	.diagram-legend span {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
	}
	.legend-line {
		display: inline-block;
		width: 1.8rem;
		height: 0;
		border-top: 2px solid var(--arc-teal);
	}
	.legend-line.dashed {
		border-top-color: #8290a3;
		border-top-style: dashed;
	}
	.legend-line.future {
		border-top-color: #b6c0cd;
		border-top-style: dotted;
		opacity: 0.75;
	}
	.legend-box {
		display: inline-block;
		width: 1.1rem;
		height: 0.8rem;
		background: #f8fafc;
		border: 1px solid #94a3b8;
		border-radius: 4px;
	}
	.dependency-graph {
		display: block;
		width: 100%;
		height: auto;
		font-family: inherit;
	}
	.layer-label {
		fill: #718096;
		font-size: 12px;
		font-weight: 800;
		letter-spacing: 0.09em;
	}
	.edge {
		fill: none;
		stroke-width: 2.5;
	}
	.edge.solid {
		stroke: var(--arc-teal);
	}
	.edge.planned {
		stroke: #8290a3;
		stroke-dasharray: 8 7;
	}
	.edge.future {
		stroke: #aab5c4;
		stroke-dasharray: 2 8;
		stroke-linecap: round;
		opacity: 0.72;
	}
	.arrow-head.solid {
		fill: var(--arc-teal);
	}
	.arrow-head.planned {
		fill: #8290a3;
	}
	.arrow-head.future {
		fill: #aab5c4;
		opacity: 0.72;
	}
	.graph-node rect {
		stroke-width: 1.5;
	}
	.graph-node.implemented rect {
		fill: #eefbf7;
		stroke: #31a884;
	}
	.graph-node.implemented.emphasized rect {
		fill: #ddf7ef;
		stroke: var(--arc-teal);
		stroke-width: 2;
	}
	.graph-node.planned rect {
		fill: #f8fafc;
		stroke: #94a3b8;
		stroke-dasharray: 7 5;
	}
	.graph-node.future rect {
		fill: #f8fafc;
		stroke: #aab5c4;
		stroke-dasharray: 3 6;
		opacity: 0.78;
	}
	.graph-node.independent rect {
		fill: #f8fafc;
		stroke: #94a3b8;
	}
	.graph-node.platform rect {
		fill: var(--arc-navy);
		stroke: var(--arc-navy);
	}
	.future-label rect {
		fill: #f8fafc;
		stroke: #cbd5e1;
		stroke-dasharray: 3 5;
	}
	.future-label text {
		fill: #64748b;
		font-size: 11px;
		font-weight: 750;
		letter-spacing: 0;
	}
	.node-title {
		fill: var(--arc-ink);
		font-size: 17px;
		font-weight: 800;
	}
	.node-meta {
		fill: var(--arc-muted);
		font-size: 12px;
		font-weight: 600;
	}
	.platform-title {
		fill: #f8fafc;
		font-size: 15px;
	}
	.dependency-list {
		display: none;
	}
	.architecture-note {
		display: grid;
		grid-template-columns: minmax(190px, 0.7fr) minmax(0, 1.7fr);
		gap: 1.5rem;
		margin: 0.25rem;
		padding: 1.35rem;
		background: #f0f7f5;
		border: 1px solid #d4e7e1;
		border-radius: 16px;
	}
	.architecture-note strong {
		color: var(--arc-teal-dark);
		font-size: 1rem;
		line-height: 1.45;
	}
	.architecture-note p {
		margin: 0;
		color: var(--arc-muted);
		font-size: 0.91rem;
		line-height: 1.6;
	}
	.evidence-grid {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 0.8rem;
		margin-top: 0.9rem;
	}
	.evidence-grid div {
		display: grid;
		align-content: start;
		gap: 0.35rem;
		min-height: 138px;
		padding: 1.1rem;
		background: rgba(255, 255, 255, 0.76);
		border: 1px solid #d5dde7;
		border-radius: 16px;
	}
	.evidence-grid strong {
		color: var(--arc-teal-dark);
		font-size: 1.38rem;
		letter-spacing: -0.035em;
	}
	.evidence-grid span {
		color: var(--arc-muted);
		font-size: 0.85rem;
		line-height: 1.45;
	}
	.principles {
		margin-top: 5rem;
		padding: 2.3rem;
		background: var(--arc-navy);
		border-radius: 24px;
	}
	.principles .eyebrow {
		color: var(--arc-mint);
	}
	.principles h2 {
		color: #f8fafc;
	}
	.principle-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
		margin-top: 2rem;
	}
	.principle-grid div {
		display: grid;
		gap: 0.3rem;
	}
	.principle-grid strong {
		color: #f8fafc;
	}
	.principle-grid span {
		color: #b9c4d8;
		font-size: 0.92rem;
		line-height: 1.5;
	}
	@media (max-width: 820px) {
		.product-grid {
			grid-template-columns: 1fr;
		}
		.product-card {
			min-height: 0;
		}
		.product-link {
			margin-top: 1.25rem;
		}
		.dependency-graph {
			display: none;
		}
		.dependency-list {
			display: grid;
			gap: 0.75rem;
			padding: 0.2rem 0 0.8rem;
		}
		.dependency-list div {
			display: grid;
			gap: 0.45rem;
			padding: 1rem;
			background: #f8fafc;
			border: 1px solid #d5dde7;
			border-radius: 14px;
		}
		.dependency-list strong {
			color: var(--arc-ink);
		}
		.dependency-list em {
			color: var(--arc-muted);
			font-size: 0.72rem;
			font-style: normal;
			font-weight: 700;
			text-transform: uppercase;
		}
		.dependency-list span {
			color: var(--arc-muted);
			font-size: 0.86rem;
			line-height: 1.55;
		}
		.architecture-note {
			grid-template-columns: 1fr;
			gap: 0.55rem;
		}
		.evidence-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}
	@media (max-width: 600px) {
		.arc-page {
			width: min(100% - 2rem, 1080px);
			padding-top: 3.5rem;
		}
		.hero {
			padding-bottom: 4rem;
		}
		.section-heading {
			display: block;
		}
		.section-heading > p {
			margin-top: 0.8rem;
			text-align: left;
		}
		.principles {
			padding: 1.5rem;
		}
		.principle-grid {
			grid-template-columns: 1fr;
		}
		.architecture-panel {
			padding: 0.9rem;
			border-radius: 18px;
		}
		.diagram-legend {
			display: grid;
		}
		.evidence-grid {
			grid-template-columns: 1fr;
		}
		.evidence-grid div {
			min-height: 0;
		}
	}

	/* ARC retains its teal product language, while its shell follows the global
	   Carbon / Operator surface tokens instead of forcing a white canvas. */
	:global(html[data-paper-theme]) .arc-stage {
		background:
			radial-gradient(42rem 26rem at 8% 8%, color-mix(in srgb, var(--apaper-accent) 18%, transparent), transparent 72%),
			radial-gradient(38rem 24rem at 94% 26%, rgba(45, 224, 168, 0.1), transparent 74%),
			var(--apaper-bg);
		border-bottom-color: var(--apaper-line);
	}
	:global(html[data-paper-theme]) .arc-page {
		--arc-ink: var(--apaper-ink);
		--arc-muted: var(--apaper-muted);
		--arc-teal: var(--apaper-accent);
		--arc-teal-dark: var(--apaper-accent);
		--arc-mint: color-mix(in srgb, var(--apaper-accent) 70%, #2de0a8);
		--arc-navy: var(--apaper-bg-alt);
	}
	:global(html[data-paper-theme]) .arc-page h1,
	:global(html[data-paper-theme]) .arc-page .section-heading h2 {
		color: var(--arc-ink);
	}
	:global(html[data-paper-theme]) .arc-page .product-card,
	:global(html[data-paper-theme]) .arc-page .architecture-panel,
	:global(html[data-paper-theme]) .arc-page .evidence-grid div,
	:global(html[data-paper-theme]) .arc-page .dependency-list div {
		background: var(--apaper-card);
		border-color: var(--apaper-line);
		box-shadow: none;
	}
	:global(html[data-paper-theme]) .arc-page .product-card.available {
		border-color: var(--apaper-accent-line);
	}
	:global(html[data-paper-theme]) .arc-page .product-card.available:hover {
		border-color: var(--apaper-accent);
		box-shadow: 0 18px 40px var(--apaper-accent-soft);
	}
	:global(html[data-paper-theme]) .arc-page .status {
		background: var(--apaper-bg-alt);
		color: var(--apaper-ink-secondary);
	}
	:global(html[data-paper-theme]) .arc-page .status.live:not(.app-store-status),
	:global(html[data-paper-theme]) .arc-page .status.review,
	:global(html[data-paper-theme]) .arc-page .status.building,
	:global(html[data-paper-theme]) .arc-page .status.planned {
		background: var(--apaper-accent-soft);
		color: var(--apaper-accent);
	}
	:global(html[data-paper-theme]) .arc-page .status.live.app-store-status {
		background: linear-gradient(145deg, #151719, #34383d);
		border-color: rgba(255, 255, 255, 0.2);
		color: #f8fafc;
	}
	:global(html[data-paper-theme]) .arc-page .architecture-note {
		background: var(--apaper-bg-alt);
		border-color: var(--apaper-line);
	}
	:global(html[data-paper-theme]) .arc-page .legend-box,
	:global(html[data-paper-theme]) .arc-page .graph-node.implemented rect,
	:global(html[data-paper-theme]) .arc-page .graph-node.implemented.emphasized rect,
	:global(html[data-paper-theme]) .arc-page .graph-node.planned rect,
	:global(html[data-paper-theme]) .arc-page .graph-node.future rect,
	:global(html[data-paper-theme]) .arc-page .graph-node.independent rect,
	:global(html[data-paper-theme]) .arc-page .future-label rect {
		fill: var(--apaper-card);
		background: var(--apaper-card);
		stroke: var(--apaper-line);
		border-color: var(--apaper-line);
	}
	:global(html[data-paper-theme]) .arc-page .future-label text,
	:global(html[data-paper-theme]) .arc-page .layer-label,
	:global(html[data-paper-theme]) .arc-page .node-meta {
		fill: var(--apaper-muted);
	}
</style>
